<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Atendimento</title>

    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
        }

        .header {
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
        }

        .senha-chamada {
            font-size: 30px;
            text-align: center;
            border: 1px solid #ccc;
            padding: 10px;
            width: 200px;
        }

        .fila {
            padding: 10px;
            border: 1px solid #ccc;
            margin-bottom: 10px;
        }

        .atendimento-container {
            border: 1px solid #ccc;
            display: flex;
            background-color: darkgrey;
            padding: 10px;
            margin-bottom: 10px;
        }

        .atendente, .cliente {
            flex: 1;
            text-align: center;
            color: white;
        }

        .button-container {
            display: flex;
            width: 100%;
            padding: 10px;
        }

        .button {
            flex: 1;
            text-align: center;
            padding: 10px;
            cursor: pointer;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
        }
    </style>

    <script>
        function proximo() {
            window.location.href = "/proximo";
        }

        function retirarSenha() {
            window.location.href = "/retirar";
        }
    </script>

</head>

<body>

    <div class="container">

        <div class="header">
            Sistema de Atendimento
        </div>

        <div class="senha-chamada">
            <p>SENHA CHAMADA:</p>
            <%= senhaAtual %>
        </div>

        <div class="fila">
            Senhas a serem chamadas: <%= (fila == null ? "" : fila) %>
        </div>

        <div class="atendimento-container">
            <div class="atendente">Atendente</div>
            <div class="cliente">Cliente</div>
        </div>

        <div class="button-container">
            <div class="button" onclick="proximo()">Chamar próximo</div>
            <div class="button" onclick="retirarSenha()">Retirar Senha</div>
        </div>

    </div>

</body>

</html>
